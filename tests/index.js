QUnit.test( "My first test", function( assert ) {
  assert.equal( message, 'Goodbye world', "Expected true!" );
});

QUnit.test( "My first test", function( assert ) {
  assert.equal( message, 'Goodbye world', "Expected true!" );
});

QUnit.test( "My first test", function( assert ) {
  assert.equal( message, 'Goodbye world', "Expected true!" );
});

QUnit.test( "My first test", function( assert ) {
  assert.equal( message, 'Hello world!', "Expected true!" );
});
